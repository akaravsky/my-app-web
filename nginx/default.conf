upstream my-app-web {
    server web:8080;
}

upstream my-app-api {
    server api:3000;
}  

server {
    listen 80;

    location / {
        proxy_pass http://my-app-web;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://my-app-api;
    }
}
